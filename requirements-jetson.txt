# Jetson / aarch64 overrides — install AFTER uv sync
#
# The standard PyPI torch (torch==2.10.0+cu130) installed by uv sync is built
# for x86_64 and has cuBLAS incompatibilities on Jetson (aarch64), causing
# CUBLAS_STATUS_INVALID_VALUE crashes in LFM2.5-350M's rotary embeddings.
#
# NVIDIA ships Jetson-specific torch wheels via the Jetson AI Lab index.
# Run this file with the .venv pip AFTER uv sync:
#
#   uv pip install -r requirements-jetson.txt
#
# This replaces only the CUDA compute stack — all other deps from uv sync remain.

--extra-index-url https://pypi.jetson-ai-lab.io/sbsa/cu130/+simple/

# Core CUDA compute stack — sbsa/cu130 builds (ARM64 + CUDA 13.0)
# jp6/cu12x indexes are for JetPack 6 devices; sbsa/cu130 is for Thor (CUDA 13.0, generic aarch64)
torch
torchaudio
torchvision

# onnxruntime-gpu has no aarch64 wheels on this index — keep the CPU onnxruntime
# that uv sync installs. Moonshine STT and intent classifier still run on CUDA
# via torch directly; only the ONNX fallback paths use onnxruntime.
