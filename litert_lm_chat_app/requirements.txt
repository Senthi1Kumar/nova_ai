fastapi==0.115.6
uvicorn[standard]==0.34.0
# python-multipart==0.0.20
pydantic-settings==2.7.1
jinja2==3.1.5
litert-lm-api
pocket-tts
scipy
soundfile
tavily-python>=0.5.0
mcp>=1.10.0
fastmcp

# Persistent memory layer (SessionJournal + DiaryCompactor + mem0 semantic recall).
# mem0 calls a local OpenAI-compatible LLM for fact extraction — we expose one
# via app/llm_shim.py that wraps LiteRT-LM, so no extra model needed.
# Postgres + pgvector extension required for semantic recall
# mem0ai>=0.1.0
# sentence-transformers>=3.0
# pgvector>=0.3
# psycopg[c,pool]>=3.3.4
